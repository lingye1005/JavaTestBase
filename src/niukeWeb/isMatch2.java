package niukeWeb;

/**
 * Created by caoxiaohong on 17/7/15.
 * 判定前面字符串是否是后面字符串的字串.
 * .代表一个任意字母.但不能为空
 * *代表任意0个或者多个前一个字母一样的字母
 *
 */
public class isMatch2 {
    public boolean isMatch(String s, String p) {
        char[] str1=s.toCharArray();
        char[] str2=p.toCharArray();
        return false;

   }
}
